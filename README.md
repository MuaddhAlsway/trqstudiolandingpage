
  # Create based on design

  This is a code bundle for Create based on design. The original project is available at https://www.figma.com/design/u49iclEReOonQjuF8sjhVc/Create-based-on-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  